# bot-state

Persisted LMTrade SQLite state for the hourly-Routine permanent run. Written
by `scripts/hourly_routine.sh` on the `claude/trading-bot-hybrid-agent-tvs90n`
code branch — do not edit `state/lmtrade.db` by hand.

This branch is intentionally separate from the code branch so hourly state
commits don't clutter the code history / PR diffs.
