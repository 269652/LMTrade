# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Env & secrets
.env
*.env
!.env.example

# Per-fork user config (see config.example.toml) — non-secret settings that
# override config/default.yaml without touching a tracked file.
/config.toml

# Runtime state (anchored to repo root so they don't shadow the
# src/lmtrade/data source package or any other same-named dir)
/data/
*.db
*.sqlite
*.sqlite3
/logs/
*.log

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/
